<%= form_with(model: universidad, local: true) do |form| %>
  <% if universidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(universidad.errors.count, "error") %> prohibited this universidad from being saved:</h2>

      <ul>
      <% universidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :universidad_codigo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :universidad_nombre, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :direccion %>
    <%= form.text_field :direccion, id: :universidad_direccion, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :casa_matriz %>
    <%= form.text_field :casa_matriz, id: :universidad_casa_matriz, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit nil, class: "btn btn-success" %>
  </div>
<% end %>
