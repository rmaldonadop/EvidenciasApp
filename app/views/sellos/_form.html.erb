<%= form_with(model: sello, local: true) do |form| %>
  <% if sello.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sello.errors.count, "error") %> prohibited this sello from being saved:</h2>

      <ul>
      <% sello.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :sello_codigo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :sello_tipo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit nil, class: "btn btn-success" %>
  </div>
<% end %>
