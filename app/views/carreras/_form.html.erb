<%= form_with(model: carrera, local: true) do |form| %>
  <% if carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrera.errors.count, "error") %> prohibited this carrera from being saved:</h2>

      <ul>
      <% carrera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :carrera_codigo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :carrera_nombre, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :jefe_carrera %>
    <%= form.text_field :jefe_carrera, id: :carrera_jefe_carrera, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :escuela_id, "Escuela" %>
    <%= form.collection_select :escuela_id, Escuela.all, :id, :nombre, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit nil, class: "btn btn-success" %>
  </div>
<% end %>
